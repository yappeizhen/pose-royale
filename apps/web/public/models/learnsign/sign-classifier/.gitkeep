This directory will hold the trained landmark classifier (model.json + weight
shards). Drop the output of `tensorflowjs_converter` in here once training is
done — see games/learnsign/TRAINING.md for the workflow.
